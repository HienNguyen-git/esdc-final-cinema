<div class="container">
    <a href="/">Back to home</a>
  <div class="card ml-auto mr-auto" style="width:50vw;height:60vh">
    <img class="card-img-top" src="/{{imagePath}}" alt="{{name}}" />
    <div class="card-body">
      <h4 class="card-title font-weight-bold">{{name}}</h4>
      <p class="card-text">{{description}}</p>
      <p class="btn btn-primary ml-auto mr-auto">${{price}}</p>
    </div>
  </div>
</div>